#include <iostream>
using namespace std;
int dn(int n)
{
	int sum = 0;
	if (n < 100)sum += n;
	else if (n >= 100 && n <= 1000)
	{
		if((n/100 - n/10%10)  == n/10%10-n%100)
	}
}
int main(void)
{
	int n, cnt=0;
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		
	}
	return 0;
}